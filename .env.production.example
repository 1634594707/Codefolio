# Public bind port for the frontend container
PUBLIC_HTTP_PORT=80

# Allowed browser origins for the backend in production
CORS_ORIGINS=http://localhost,http://your-domain.com,https://your-domain.com
